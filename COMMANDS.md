# KRAKEN — Commands (Legacy)

**This file is superseded by [docs/commands.md](docs/commands.md).**

The command list previously documented here (`/ping`, `/clan`, `/player`, `/leaderboard`, `/donations`, `/war`, `/warlog`, `/activity`, `/recent`, `/review`) no longer exists in the codebase. It was replaced by a single tabbed command center, `/ops`, plus the Recruit HQ subsystem (`/apply`, `/status`, `/recruit-*` commands, appeals, waitlist, breaks).

See [docs/commands.md](docs/commands.md) for the current, accurate command and feature reference.
